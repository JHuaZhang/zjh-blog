name: Deploy Docs to GitHub Pages & Server

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'pnpm'

      - name: Install Dependencies
        run: pnpm install

      - name: Build Documentation Site
        run: pnpm run docs:build-site

      - name: Clean Invalid Filenames (Fix Colon Issue)
        run: |
          echo "🔧 清理文件名中的非法字符（冒号）..."
          find dist -depth -name '*:*' | while read item; do
            new_item=$(echo "$item" | tr ':' '_')
            mv "$item" "$new_item"
            echo "📝 重命名: $item → $new_item"
          done

          echo "✅ 验证清理结果..."
          if find dist -name '*:*' | grep -q .; then
            echo "❌ 仍有包含冒号的文件"
            find dist -name '*:*'
            exit 1
          else
            echo "🎉 所有文件名已清理干净"
          fi

      - name: Verify Build Output
        run: |
          if [ ! -d 'dist' ]; then
            echo '❌ Build output directory dist not found!'
            exit 1
          else
            echo '✅ Build output verified in dist/'
            echo "📂 文件数量：$(find dist -type f | wc -l)"
          fi

      - name: Setup GitHub Pages
        uses: actions/configure-pages@v4

      - name: Upload Artifact for GitHub Pages
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

      - name: Upload Build Artifact for Server
        uses: actions/upload-artifact@v4
        with:
          name: site-dist
          path: dist/
          retention-days: 1

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      pages: write
      id-token: write
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4

  deploy-to-server:
    needs: build
    runs-on: ubuntu-latest
    concurrency: deploy-to-server
    steps:
      - name: Download Build Artifact
        uses: actions/download-artifact@v4
        with:
          name: site-dist
          path: ./dist-to-deploy

      - name: Verify Downloaded Files
        run: |
          echo '📦 已下载构建产物'
          echo "📊 总文件数：$(find ./dist-to-deploy -type f | wc -l)"

      - name: Setup SSH Connection
        run: |
          mkdir -p ~/.ssh

          echo '${{ secrets.SSH_PRIVATE_KEY }}' > ~/.ssh/id_rsa
          chmod 600 ~/.ssh/id_rsa

          echo '🔍 验证私钥格式...'
          if ! head -1 ~/.ssh/id_rsa | grep -q 'BEGIN.*PRIVATE KEY'; then
            echo '❌ 错误：私钥缺少 BEGIN 标记行'
            head -2 ~/.ssh/id_rsa
            exit 1
          fi

          if ! tail -1 ~/.ssh/id_rsa | grep -q 'END.*PRIVATE KEY'; then
            echo '❌ 错误：私钥缺少 END 标记行'
            tail -2 ~/.ssh/id_rsa
            exit 1
          fi

          echo '✅ 私钥格式正确'

          ssh-keyscan -H '${{ secrets.SERVER_HOST }}' >> ~/.ssh/known_hosts

          echo '🔗 测试 SSH 连接...'
          if ssh -o BatchMode=yes '${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}' 'echo SSH连接成功'; then
            echo '✅ SSH 连接测试通过'
          else
            echo '❌ SSH 连接失败'
            exit 1
          fi

      # 关键修复：完全使用独立的环境执行 rsync
      - name: Deploy with rsync (Fixed)
        run: |
          echo '🚀 开始同步文件到服务器...'
          echo "目标路径：${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.TARGET_PATH }}"

          # 确保在全新的 shell 环境中执行
          /bin/bash -c "
            # 安装 rsync（确保可用）
            sudo apt-get update -qq
            sudo apt-get install -y rsync >/dev/null 2>&1
            
            # 验证 rsync 命令
            if ! command -v rsync &> /dev/null; then
              echo '❌ rsync 命令不存在'
              exit 1
            fi
            
            # 显示 rsync 版本
            echo '🔍 rsync 版本：'
            rsync --version | head -1
            
            # 执行同步
            echo '🔄 正在同步文件...'
            rsync -avz \
              --progress \
              --delete \
              ./dist-to-deploy/ \
              '${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}:${{ secrets.TARGET_PATH }}/'
            
            echo '✅ 文件同步完成'
          "

          # 额外的验证
          echo '🔍 验证文件是否成功同步...'
          if [ $? -eq 0 ]; then
            echo '🎉 rsync 命令执行成功！'
          else
            echo '❌ rsync 命令执行失败'
            exit 1
          fi

      - name: Verify Server Deployment
        run: |
          echo '🔍 验证服务器部署...'
          ssh '${{ secrets.SERVER_USER }}@${{ secrets.SERVER_HOST }}' "
            echo '========== 部署验证 =========='
            echo '服务器时间: \$(date)'
            echo '部署目录: ${{ secrets.TARGET_PATH }}'
            echo '目录大小: \$(du -sh ${{ secrets.TARGET_PATH }})'
            echo '文件数量: \$(find ${{ secrets.TARGET_PATH }} -type f | wc -l)'
            echo '示例文件:'
            ls -la '${{ secrets.TARGET_PATH }}' | head -5
            echo '=============================='
          "

          echo '🎉 部署验证完成！'
